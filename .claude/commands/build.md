Run a clean build and report the result.

```bash
./gradlew clean build
```

If the build fails, show the failing task and the first error. Do not attempt to fix anything — just report what failed and where.
