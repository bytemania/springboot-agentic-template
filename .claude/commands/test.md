Run the unit test suite and report the result.

```bash
./gradlew test
```

Report: total tests, passed, failed, skipped. If any tests fail, show the failing test name and the assertion error. Do not attempt to fix anything — just report.
