Show the current git working tree status.

```bash
git status
git diff --stat
```

Summarize in plain English: which files are modified, staged, or untracked. If there are staged changes, mention they are ready to commit. Keep it short — one or two sentences.
